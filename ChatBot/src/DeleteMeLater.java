
public class DeleteMeLater {

    //  FuzzyMatching.Contains("Mary went to the market and bought a bunch of flowers to show Jesse",
    //          "", .15, 5);

    //  IORW.writeLine(FuzzyMatching.IndexOfEndOfMatch("I want to travel to las vegas",
    //       "traaavel to",.15));



    // Checks if the token collection contains one of the phrases in the input list
//    public boolean containsAnyPhrase(List<String> phrases) {
//        List<List<String>> phraseList = ParserDictionary.getTokenizedPhraseList(phrases);
//
//        // Check each phrase
//        for (List<String> phrase : phraseList) {
//            //if (containsTokenizedPhrase(phrase)) return true;
//            if (fuzzyContains(tokenCollection.getStrippedInput(), phrase)) return true;
//        }
//
//        return false;
//    }



    //public boolean containsPhrase(String phrase) {
    //return containsTokenizedPhrase(Regex.tokenizeOnWhitespace(phrase));
    // }

//    private boolean containsTokenizedPhrase(List<String> phrase) {
//
//    }

    // Checks if the token collection contains a phrase (list of words)
//    private boolean containsTokenizedPhrase(List<String> phrase) {
//        List<Token> tokens = tokenCollection.getTokens();
//
//        int numTokensInMessage = tokens.size();
//        int numTokensInPhrase = phrase.size();
//
//        // loop over possible placements of the phrase in the user message
//        for (int i = 0; i < (numTokensInMessage - numTokensInPhrase); i++) {
//
//            // Extract from user message an equal number of words as there are in phrase
//            for (int j = 0; j < numTokensInPhrase; i++) {
//            }
//        }
//
//        return false;
//    }



}
